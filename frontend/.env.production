# Backend origin for production builds. Left empty here — set VITE_API_URL in
# the Vercel project settings (it overrides this at build time). If left unset,
# the app falls back to http://localhost:8000.
VITE_API_URL=
